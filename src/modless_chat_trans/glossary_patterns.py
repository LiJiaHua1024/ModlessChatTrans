"""术语表变量语法，供整句匹配和 LLM 术语筛选共用。"""

import re


# 正则中的转义、字符类和花括号量词作为整体读取，避免将其中的 } 当作变量结束。
VARIABLE_PATTERN = re.compile(
    r"\{\{([a-zA-Z0-9_-]+)"
    r"(?::((?:\\.|\[(?:\\.|[^\]\\])*\]|\{\d+(?:,\d*)?\}|(?!\}\})[^\\\[])+))?"
    r"\}\}"
)
